server:
  port: ${SERVER_PORT}

spring:
  application:
    name: udehnih-payment
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: ${SHOW_SQL:false}
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret-key: ${JWT_SECRET_KEY}
  expiration: ${JWT_EXPIRATION}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION}

services:
  auth:
    baseurl: ${AUTH_SERVICE_URL_LOCAL}
    
  course:
    baseurl: ${COURSE_SERVICE_URL_LOCAL}
    api-key: ${COURSE_SERVICE_API_KEY:}
    
  dashboard:
    baseurl: ${DASHBOARD_SERVICE_URL_LOCAL}
    api-key: ${DASHBOARD_SERVICE_API_KEY:test-api-key}